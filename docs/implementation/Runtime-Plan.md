# Runtime Plan

## Current Focus

- normalize theme resolver behavior
- bind token registry to ID registry
- define runtime enforcement hooks
- expand registry object graph

## Near-Term Tasks

- make theme resolution registry-aware
- define valid token lookup paths
- create explicit runtime validation flow
- document control-center-to-subsystem handoff rules
