/**
 * Upload Contract
 * Defines upload validation authority.
 */

export const UploadContract = {
  svg: {
    extensions: [".svg"],
    requiredMetadata: [
      "name",
      "category"
    ]
  }
};
