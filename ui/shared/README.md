# UI Shared

This directory holds shared UI primitives for dashboard, editor, and settings surfaces.

## Scope

- shared shell structure
- shared section framing
- shared runtime status views
- shared panel composition rules

## Current State

Skeleton created. Shared UI primitives still need implementation.
